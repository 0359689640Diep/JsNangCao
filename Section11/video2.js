const arr = [23, 11, 64];
// lấy phần tử ở vị trí đầu tiên của mảng
console.log(arr.at(0));
// lấy phần tử ở cuối mảng
console.log(arr[arr.length -1]);
// lấy phần tử ở cuối mảng và đưa nó thành một mảng mới
console.log(arr.slice(-1));
console.log(arr);